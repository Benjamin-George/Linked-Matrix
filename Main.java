public class Main {

    public static void main(String[] args) {

        //===================================TEST LINKED MATRIX====================================//

        /*
        MyLinkedMatrix intMatrix = new MyLinkedMatrix();
        int rows = 3;
        int cols = 3;
        System.out.println("Initializing intMatrix with " + rows + " rows and " + cols + " columns.");
        System.out.println("");
        intMatrix.init(rows, cols);

        System.out.println("Printing intMatrix");
        for (int i = 1; i <= rows; i++) {
            String rowString = "";
            for (int j = 1; j <= cols; j++) {
                rowString += intMatrix.getValue(i, j) + ", ";
            }
            System.out.println("Row " + i + " is " + rowString);
        }

        System.out.println("");
        int getRow = 1;
        int getCol = 1;
        System.out.println("The value at (" + getRow + ", " + getCol + ") is: " + intMatrix.getValue(getRow, getCol));

        System.out.println("");
        int rowToSum = 1;
        int colToSum = 1;
        System.out.println("The sum of row " + rowToSum + " is " + intMatrix.sumRow(rowToSum));
        System.out.println("The sum of column " + colToSum + " is " + intMatrix.sumCol(colToSum));

        System.out.println("");
        int setRow = 2;
        int setCol = 2;
        int setNum = 80;
        System.out.println("Setting the value at (" + setRow + ", " + setCol + ") to " + setNum);
        intMatrix.setValue(setNum, setRow, setCol);
        System.out.println("intMatrix is now:");
        for (int i = 1; i <= rows; i++) {
            String rowString = "";
            for (int j = 1; j <= cols; j++) {
                rowString += intMatrix.getValue(i, j) + ", ";
            }
            System.out.println("Row " + i + " is " + rowString);
        }

        System.out.println("");
        int fillNum = 7;
        System.out.println("Filling all nodes in the matrix with " + fillNum);
        intMatrix.fill(fillNum);
        System.out.println("intMatrix is now:");
        for (int i = 1; i <= rows; i++) {
            String rowString = "";
            for (int j = 1; j <= cols; j++) {
                rowString += intMatrix.getValue(i, j) + ", ";
            }
            System.out.println("Row " + i + " is " + rowString);
        }
        */

    }
}
